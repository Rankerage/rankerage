# Rankerage.com 테이블 개편 명세

## 배경
- 197개국 × ~250개 순위 지표의 대형 비교 테이블 (Tabulator 5.5.2)
- 기본 정렬: election_days asc (절대값, 임박 선거 우선)
- null 데이터 국가는 정렬 제외 → 표 최하단
- 개인/기업/기관 데이터는 entities_archive.json으로 분리 완료 (표에서 제거됨)

## 요구사항

### 1. 모든 셀 공통 원칙
- **표시값**: 셀에 순위값 표시 (예: 인구 33.0M)
- **마우스오버**: 순위 제목 + 실제 순위 (예: "인구 — #46")
- **마우스클릭**: 웹앱(모달)으로 과거 여러 시점의 수치+순위 꺾은선 그래프

### 2. 선거(election) 열 — 특별 취급
- **표시값**: 선거일 (MM/DD)
- **순위**: |선거일 - 오늘| 작을수록 앞선 순위
- **마우스오버**: 선거 제목 (예: "대통령 선거")
- **마우스클릭**: 웹앱에 제목 + 지지율 최신 뉴스 요약 + 출처
- **매일 업데이트** (기존 cron: 매일 06:00)

### 3. 업데이트 주기
- 각 순위의 특성을 감안해 AI가 주기를 정하고 조정 (선거=매일, FIFA=매월, WorldBank=분기 등)

### 4. 기본 보기
- 최우선 = election, 새로고침하면 election 순위로 복귀

### 5. 정렬 규칙
- 표시값 없는 국가: 정렬 제외 → 최하단
- 순위제목 클릭: 1회=정순, 2회=역순 (토글)

### 6. 검색
- 사용자 입력 문자와 관련된 순위제목 전부를 펼침목록(dropdown)으로 표시
- 선택하면 해당 순위를 3번째 열(최상단)로 끌어와 정렬된 상태로 표시
- 미등록 순위면 즉석 조회, 10개국 이상 데이터 있으면 정식 등록 (추후 기능)

### 7. 스크롤 성능
- 세로 스크롤 + shift+wheel 가로 스크롤 원활
- 가상 스크롤로 화면상 데이터 우선 렌더링 (느리지 않게)

## 현재 구현 상태 (완료된 것)
- election_days asc 기본 정렬 + initialSort ✓
- null → 하단 (S/E 소터) ✓
- 컬럼 클릭 asc/desc 토글 ✓
- shift+wheel 가로 스크롤 ✓
- election 셀: 날짜 표시 + 툴팁(선거명) ✓
- 트렌드 그래프 (chartModal + history.json, 95개 WorldBank 지표) — 부분 구현
- 검색 드롭다운 (searchDropdown) — 기존 존재

## 미구현 / 개선 필요
- [ ] 셀 마우스오버에 "순위제목 + 실제순위" 툴팁 (현재 값만 표시)
- [ ] 셀 클릭 → 꺾은선 그래프 (모든 지표, 현재 history.json 95개만)
- [ ] 선거 클릭 → 지지율 뉴스 요약 + 출처 (현재 "준비 중" placeholder)
- [ ] 검색 드롭다운 개선 (관련 순위제목 전부 표시 + 선택 시 3열로 이동)
